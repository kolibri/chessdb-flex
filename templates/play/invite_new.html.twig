{% extends 'layout.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">{{ 'form.create_invite.label.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
