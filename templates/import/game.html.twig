{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">{{ 'form.game.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
